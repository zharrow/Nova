{
  "name": "nova-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Nova — librairie de composants animés, framework-agnostique au cœur, distribuée par registry.",
  "packageManager": "pnpm@10.26.1",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "registry:build": "tsx scripts/build-registry.ts",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist apps/*/node_modules apps/*/.next"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
